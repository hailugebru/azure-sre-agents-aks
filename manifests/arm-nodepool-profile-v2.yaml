---
# Custom AKSNodeClass for Azure Linux
apiVersion: karpenter.azure.com/v1alpha2
kind: AKSNodeClass
metadata:
  name: default-azurelinux
spec:
  imageFamily: AzureLinux
---
# NAP/Karpenter NodePool – ARM64 D-family VMs on Azure Linux
apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: arm
spec:
  weight: 10
  template:
    spec:
      nodeClassRef:
        group: karpenter.azure.com
        kind: AKSNodeClass
        name: default-azurelinux
      requirements:
        - key: kubernetes.io/arch
          operator: In
          values:
            - arm64
        - key: karpenter.azure.com/sku-family
          operator: In
          values:
            - D
        - key: karpenter.sh/capacity-type
          operator: In
          values:
            - on-demand
