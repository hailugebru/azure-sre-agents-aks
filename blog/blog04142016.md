# Autonomous AKS Incident Response with Azure SRE Agent: Alert to Recovery in ~8 Minutes

**Tags:** Azure SRE Agent, AI Ops, Agentic AI, Incident Response, AKS, KEDA, Azure

---

## The 2 AM problem every SRE team knows

A Sev1 alert on AKS still often means the same familiar sequence: wake an engineer, correlate logs and metrics, inspect cluster state, apply a mitigation, verify recovery, and document the incident. This post shows what that workflow looked like when Azure SRE Agent handled it end-to-end in a controlled, production-like AKS lab.

That is the value proposition here: not just faster incident response, but a more consistent one. Azure SRE Agent can receive incidents, investigate Azure-native telemetry, reason over infrastructure state, execute mitigations when allowed, verify the outcome, and notify the team through systems such as Microsoft Teams and GitHub.

## Why this matters

The key point is not just that the agent investigated the issue. It carried the workflow from incident intake through remediation, verification, and engineering follow-up.

| Metric | Traditional on-call | This demo with Azure SRE Agent |
|---|---|---|
| Alert to recovery | 60–120 min | **~8 min** |
| Human interventions | Multiple | **0** |
| Scope of investigation | Manual, service by service | **6 namespaces / 40+ pods scanned automatically** |
| Correlate evidence and diagnose | 10–15 min | **~2 min** |
| Apply fix and verify | 15–30 min | **~4 min** |
| Post-incident follow-up | Manual next-day task | **GitHub issue created automatically; source fix prepared as a draft PR for human review** |

> _These outcomes reflect a controlled lab run on April 10, 2026. Actual results will vary based on telemetry latency, cluster size, alert quality, and incident type._

The key value is not just speed. It is **consistency**: the same investigate → diagnose → remediate → verify → report loop every time, whether it is 2 AM or 2 PM.

---

## Azure SRE Agent on AKS in one minute

Azure SRE Agent is a governed incident-response system, not just a conversational assistant. Five concepts matter most in an AKS incident workflow:

1. **Incident platform** — where incidents originate. In this demo, that is Azure Monitor.
2. **Built-in Azure capabilities** — the agent can use Azure Monitor, Log Analytics, Azure Resource Graph, Azure CLI/ARM, and AKS diagnostics without requiring external connectors.
3. **Connectors** — these extend the workflow to systems such as GitHub, Teams, Kusto, and MCP servers.
4. **Permission levels** — `Reader` for investigation and read-oriented access, `Privileged` for operational changes when allowed.
5. **Run modes** — `Review` for approval-gated execution and `Autonomous` for direct execution.

That model matters more than any single prompt. The most important production controls are permission level and run mode. Custom instructions can shape workflow behavior, but they do not replace RBAC, telemetry quality, or tool availability.

The safest production rollout path is:

```text
Start:   Reader + Review
Then:    Privileged + Review
Finally: Privileged + Autonomous for narrow, trusted incident paths
```

---

## Demo environment

If you want to reproduce the demo, the full scripts and manifests are here:


> **Demo repository:** [github.com/hailugebru/azure-sre-agents-aks](https://github.com/hailugebru/azure-sre-agents-aks)  
> The README includes the full setup and configuration details.

The environment uses an AKS cluster with node auto-provisioning (NAP), Azure CNI Overlay powered by Cilium, managed Prometheus metrics, the AKS Store sample application, and Azure SRE Agent configured for incident-triggered investigation and remediation. NAP automatically deploys and manages Karpenter on AKS, and the AKS Store sample is a polyglot microservices application designed for AKS demos and experiments. That is enough to create a realistic AKS environment for the incident flows below. For full demo setup details, see the README.

```text
Azure Monitor  →  Action Group  →  Azure SRE Agent  →  AKS Cluster
(Metric alert)    (Webhook)        (Investigate +       (Diagnose +
                                    Fix + Verify)        Remediate)

                                      ↓
                           Teams notifications + GitHub issue + GitHub Agent analyzes issue → PR opened for review.
```
---

## How Azure SRE Agent was configured for this demo

For this demo, Azure SRE Agent configuration came down to four things: scope, permissions, incident intake, and response mode. I scoped the agent to the demo resource group and used its user-assigned managed identity (UAMI) for Azure access. That scope defined what the agent could investigate, while RBAC determined what actions it could take. 

I used broader AKS permissions than I would recommend as a default production baseline so the agent could complete remediation end to end in the lab. That is an important distinction: permissions control what the agent can access, while run mode controls whether it asks for approval or acts directly. For this scenario, Azure Monitor served as the incident platform, and I set the response plan to Autonomous for a narrow, trusted path so the workflow could run without manual approval gates.

I also added Teams and GitHub integrations so the workflow could extend beyond Azure itself: Teams for milestone updates during the incident and GitHub for durable follow-up after remediation. The agent’s built-in Azure capabilities handled the core investigation; the integrations mainly extended communication and tracking. Custom instructions helped shape the AKS pod-health workflow, but the real production controls were still scope, RBAC, telemetry quality, and run mode. For the complete Azure SRE Agent setup, see the README.

---

## Two common AKS incident patterns, two response modes

These incidents happened on the same cluster in one session and show the two most common ways teams use Azure SRE Agent on AKS:

1. **Alert-triggered automation** — the agent acts when Azure Monitor fires.
2. **Ad hoc chat investigation** — an engineer notices something wrong and hands it off via chat.

### Incident 1 — CPU starvation (alert-driven, ~8 min MTTR)

I intentionally patched `makeline-service` to an unusably low CPU limit:

```powershell
kubectl patch deployment makeline-service -n pets --type='json' `
  -p='[
    {"op":"replace","path":"/spec/template/spec/containers/0/resources/limits/cpu","value":"5m"},
    {"op":"replace","path":"/spec/template/spec/containers/0/resources/limits/memory","value":"20Mi"}
  ]'
```

Within five minutes, Azure Monitor fired the `pod-not-healthy` Sev1 alert. The agent picked it up immediately.

Here is the key diagnostic conclusion the agent reached from the pod state, probe behavior, and exit code:

> *"Exit code 1 (not 137) rules out OOMKill. The pod failed at startup, not at runtime memory pressure. CPU limit of 5m is insufficient for the process to bind its port before the startup probe times out. This is a configuration error, not a resource exhaustion scenario."*

That single inference, distinguishing a startup probe timeout from an OOMKill, is the kind of domain reasoning that takes an on-call engineer 10 minutes to work through at 2 AM. The agent reached it in under 90 seconds.

The agent then:

- found three more pods CPU-throttled at 112–200% of their limit using `kubectl top`,
- patched four workloads: `makeline-service`, `virtual-customer`, `virtual-worker`, and `mongodb`,
- verified all nine pods returned to Running with 0 restarts cluster-wide.

> _[Screenshot: Azure SRE Agent — Incident History blade showing alert ID `69e4dbba`, Status: Resolved, 4 patches applied, cluster-wide sweep: 0 unhealthy pods.]_

**Outcome:** Full cluster recovery in about 8 minutes. Human interventions: 0.

### Incident 2 — OOMKilled (chat-driven, ~4 min MTTR)

I then deployed a deliberately under-sized version of `order-service`:

```powershell
kubectl apply -f .\manifests\aks-store\order-service-changed.yaml -n pets
```

I intentionally started the second case from chat before the pod-phase alert fired to demonstrate the interactive troubleshooting flow. That was a demo choice, not an alerting gap. `CrashLoopBackOff` is a container waiting reason, not a pod phase, so production coverage should come from Prometheus-based crash-loop signals rather than pod phase alone.

```text
The order-service pod in the pets namespace is not healthy.
Please investigate, identify the root cause, and fix it.
```

The agent’s reasoning:

> *"Container logs are empty. The process was killed before it could write its first log line. Exit code 137 confirms OOMKill. No NODE_OPTIONS in the ConfigMap rules out a V8 heap misconfiguration. The 20Mi limit is 12.8× below the pod’s observed 50Mi runtime baseline. This limit was never viable for this workload."*

The agent increased the memory limit (`20Mi → 128Mi`) and request (`10Mi → 50Mi`), then verified the new pod stabilized at 74Mi/128Mi (58% utilization) with 0 restarts.

> _[Screenshot: Azure SRE Agent — chat window showing investigation thread and patch confirmation for `order-service`.]_

**Outcome:** Service recovered in about 4 minutes without any manual cluster interaction.

---

## Teams + GitHub follow-up

After Incident 1 resolved, Azure SRE Agent created a GitHub issue with the remediation details. I then assigned that issue to GitHub Copilot agent, which analyzed the repository and opened a draft pull request to align the source manifests with the incident hotfix for my review. This extended the workflow beyond runtime remediation: Azure SRE Agent fixed the live issue, and GitHub Copilot cloud agent prepared the durable source change so future deployments would not reintroduce the same configuration problem. Human review still remained the final control point before merge. 

>_[Screenshot: Draft pull request created by GitHub Copilot cloud agent showing order-service memory request/limit updates to match the incident hotfix.]_

I also added a Microsoft Teams connector so Azure SRE Agent could post milestone updates as the autonomous workflow progressed. During the incident, the agent sent three channel messages:

    investigation started,
    root cause and remediation identified,
    incident resolved.

Teams gave the on-call team real-time visibility, while GitHub captured both the durable follow-up issue and the draft source fix for review. For GitHub integration details, see the README.

---

## Key takeaways

In this AKS lab, Azure SRE Agent handled two common incident patterns—one alert-driven and one chat-driven—applying targeted remediation, verifying recovery, posting Teams updates, and creating a GitHub issue for follow-up. The loop also continued into source control: after the incident, the generated GitHub issue was assigned to GitHub Copilot agent, which analyzed the repository and opened a draft pull request to align the source manifests with the production hotfix for human review.

### Three things to carry forward

1. **Azure SRE Agent is a governed incident-response system, not just a chatbot.** The most important controls are permission levels and run modes, not prompt quality.
2. **Built-in Azure diagnostics cover most of the core workflow.** Use connectors to extend the workflow outward, for example Teams for real-time coordination and GitHub for durable engineering follow-up.
3. **Start narrow and expand deliberately.** One resource group, one incident type, `Review` first. Validate that telemetry flows, RBAC is scoped correctly, and your alert rules cover the failure modes you actually care about before enabling `Autonomous`.

### Next steps

- Add alert coverage for `ImagePullBackOff` and node resource pressure to complement the pod phase rule.
- Expand to multi-cluster managed scopes once the single-cluster path is trusted and validated.

### Resources

| Resource | Link |
|---|---|
| Demo repository | [github.com/hailugebru/azure-sre-agents-aks](https://github.com/hailugebru/azure-sre-agents-aks) |
| Azure SRE Agent docs | [learn.microsoft.com/azure/sre-agent](https://learn.microsoft.com/azure/sre-agent/) |
| AKS Store Demo | [github.com/Azure-Samples/aks-store-demo](https://github.com/Azure-Samples/aks-store-demo) |
| Node Auto-Provisioning | [learn.microsoft.com/azure/aks/node-autoprovision](https://learn.microsoft.com/en-us/azure/aks/node-auto-provisioning) |
| KEDA on AKS | [learn.microsoft.com/azure/aks/keda-about](https://learn.microsoft.com/azure/aks/keda-about) |
